# Design

本檔記錄的是「已經蓋出來的東西」，不是計畫。要改設計就改這裡，然後讓程式碼跟上。

## 世界

**台灣多聯式複寫單據。**

不是儀表板，不是理財小幫手。每一頁是一張可以逐行核對的單據：有抬頭、有資料版本、有要填的空格、有算式抽屜、有蓋章。這個選擇來自產品最核心的主張——「每個數字都能攤開看公式與法源」——而複寫聯正好是台灣人最熟悉的「同一筆資料的多個版本並存」的實體形式，所以它同時也是情境比較的機制。

刻意拒絕的兩個品類預設：深色霓虹的 fintech 儀表板，以及米色圓角的消費級理財 App。

## 色彩

策略是 **Restrained**：紙與墨的中性底，加一個強調色。

| 角色 | 代幣 | 日間 | 夜間 |
|---|---|---|---|
| 檯面 | `--paper` | `#F2F2EE` | `#101318` |
| 單據 | `--sheet` | `#FBFBF8` | `#191D24` |
| 凹陷區（輸入槽、表頭） | `--sheet-sunk` | `#EAEAE4` | `#12161C` |
| 主墨 | `--ink` | `#15181B` | `#E9EAE4` |
| 次墨 | `--ink-2` | `#454A50` | `#B4B8BE` |
| 三級墨（說明） | `--ink-3` | `#5F656C` | `#8A9099` |
| 強調（印泥靛藍） | `--accent` | `#123A72` | `#6DA4EE` |
| 語意・增加／流入／漲 | `--up` | `#B4232C` | `#F0616B` |
| 語意・減少／流出／跌 | `--down` | `#0C6B44` | `#45C48C` |
| 警戒 | `--warn` | `#8A5A00` | `#E0A93F` |
| 硃砂印 | `--stamp` | `#B8342A` | `#E4614E` |
| 作廢／未查證 | `--stamp-void` | `#646A72` | `#848C97` |

**四聯紙色**（`--ply-1` 到 `--ply-4`：白、淡黃、淡粉、淡藍）標示並存情境，也在首頁當作每個工具的分類身分。

三條不可違反的規則：

1. `--accent` 是唯一的強調色。主要動作、選取狀態、焦點環都用它。
2. **紅綠是語意色，不是紅綠燈。** 紅＝增加／流入／漲，綠＝減少／流出／跌（台灣制）。不准拿來表達「好／壞」，因為同一頁上另一個元件正在用它表達「流入／流出」，兩套意思會打架。嚴重程度用硃砂章與文案表達。
3. 圖表序列色 `--series-1..5` 與語意色分離，避免多標的折線被誤讀成漲跌。

所有前景／背景組合都驗過 WCAG AA：內文 ≥4.5:1、大字 ≥3:1。`--ink-3` 對全部十種紙色皆 ≥4.55:1。

## 字體

- 中文與介面：**Noto Sans TC**（400/500/700/900）
- 數字與資料：**Martian Mono**，寬度軸壓到 `font-stretch: 75%`

75% 不是為了好看，是為了對齊與容納：那是收據與帳票印表機的字寬，六位數金額才不會把讀數列撐破。所有數字一律 `font-variant-numeric: tabular-nums`。

字級是固定 rem 級距（比率約 1.2），不是流體。Operate 介面在不同視窗下應該保持一致的密度，流體字級只會讓側欄裡的標題縮成怪樣子。唯一的例外是首頁封面大標與 `.section > h2`，它們是 Persuade 版面。

```
--t-xs 12 / --t-sm 13 / --t-base 15 / --t-md 17 / --t-lg 20
--t-xl 24 / --t-2xl 30 / --t-3xl 38 / --t-4xl 48 / --t-5xl 64
```

## 形狀與深度

**圓角一律 0。** 唯一的曲線來自硃砂章與圖表上的資料點。這條鎖是整個世界最容易被破壞也最有辨識度的一條，`--radius: 0` 存在只是為了讓違規顯眼。

陰影帶位移與柔邊（`--lift-1/2/3`），從不使用零位移的彩色光暈。夜間換成更深的黑加一條頂部高光，模擬檯燈下的紙。

間距是 4px 基準；表格列高 `--row: 2.25rem`，所有帳列表對齊同一條格線。中文內文行長 `--measure: 34em`（約 40 字）。

## 元件

容器只有一種：`.sheet`（單據）。**不用卡片。** 分組用 `border-top`、髮絲線與留白。

| 元件 | 說明 |
|---|---|
| `.sheet` / `.sheet__head` / `.sheet__body` / `.sheet__foot` | 單據。表頭永遠帶 `.datestamp` 資料版本 |
| `.plies` / `.ply` | 四聯。情境比較的主要機制，切換時舊情境留在圖上當鬼影 |
| `.field` | 填寫格。底線式、右對齊數字、label 在上、錯誤在下。不用 placeholder 當標籤 |
| `.slider` | 本套件最重要的輸入。每個都必須有等效的數字孿生（`.slider__twin`）與 `aria-valuetext` |
| `.segmented` | 勾選格 |
| `.btn` | 方形按鈕。載入中不用轉圈圈，用逐格推進的進紙條 |
| `.ledger` | 帳列表。表頭 sticky，長表用 `virtualTable` 虛擬捲動 |
| `.readout` | 主要結論數字，用 `makeCounter` 驅動 |
| `.verdict` + `.stamp` | 結論卡與硃砂章。一頁只有一個 |
| `.formula` | 算式抽屜。逐行展開代入值與法源條號 |
| `.plot` | 圖表框。`plot__foot` 同時是圖的文字結論，不讓視覺是唯一通道 |
| `.note` | 說明條。頂線 3px，**不用彩色粗左框** |
| `.state--refuse` | 拒答。資料不足時顯示這個，不畫假的分布 |

圖示全部自繪（`ui.js` 的 `ICONS`），單一線寬 1.75、方端點。不用 emoji、不用 Unicode 符號代替圖示。

## 動效

規則只有一條：**每一個動作都必須能用一句話說明它讓使用者理解了什麼。** 說不出來就刪掉。

一頁只花一個招牌動效，就是那個 App 的核心論證。其餘位置保持安靜。

| 語彙 | 它讓使用者理解什麼 |
|---|---|
| `printRows` 逐行推出 | 結果是被算出來並列印的，同時建立閱讀順序 |
| `carbonTransfer` 複寫壓力傳遞 | 我改了上面的欄位，下面哪些數字跟著變 |
| `stampIn` 蓋章 | 結論落定 |
| `makeCounter` 數字滾動 | 變化的幅度被感覺到，而不只是被讀到 |
| `tearOff` 撕開騎縫線 | 分岔出一個新情境是一個實體動作 |
| `revealOnScroll` | 長頁面上這一段是新的 |
| `flagCross` | 剛剛越過了一條有意義的線 |

技術面：只補間 `transform` 與 `opacity`，不補間 `width`／`height`／`padding`。高頻更新（拖曳滑桿）用 `gsap.quickTo`。CustomEase `paper` 是紙落定的曲線（快速起步、長尾收束）。

`still()` = `prefers-reduced-motion` 或分頁在背景。兩種情況都直接寫入最終值——**分頁在背景時 rAF 不會跑，動畫會卡在起始值，數字正確永遠比動畫重要。**

## 版面

- `.wrap` 最大 78rem
- `.workbench` 是七個工具的骨架：桌機左欄 23rem 填寫（sticky）、右欄輸出；手機單欄且**招牌視覺排在填寫之前**（用 `order`，不換成 flex——換成 flex 會繼承 `align-items: start` 把整張表單壓成一行字寬）
- 多欄填寫區用 **container query** 而非 media query。窄側欄在寬螢幕上會被 media query 撐破
- 375–390px 是驗收現場，不是降級目標

## 驗收

```bash
node tools/probe.mjs "<url>" 390     # scrollW 必須等於 innerWidth
node tools/shot.mjs .shots "<url>"   # 桌機與手機全頁截圖
node <impeccable>/scripts/detect.mjs --json <files>
```

出貨時全部八頁的狀態：橫向溢出 0、可見文字裡的破折號 0、非零圓角 0、console 錯誤 0、detector findings 0。
