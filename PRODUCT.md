# Product

<!-- impeccable:product-schema 1 -->

> 產出方式聲明：使用者明確要求「過程中不要問使用者問題」，因此本檔案未經人工訪談，全部內容由使用者原始需求（金融理財投資、台灣、GitHub Pages 線上網站）加上一輪 14 個代理人的市場調研（2026-08-03）推導而來。所有推導事實在下方以 `[推導]` 標記；`[已驗證]` 表示以 curl／實際抓取確認過。

## Platform

web

## Stack

delegated：純靜態 HTML / CSS / 原生 JavaScript（ES modules），零建置步驟，GSAP 3.15 以 vendored 檔案內嵌於 repo。理由：部署目標是 GitHub Pages，沒有伺服器與建置管線；零建置讓每個子專案都是可獨立開啟的單一 HTML，長期不會因為工具鏈腐化而失效。市場資料以 Node 建置腳本離線抓取後 committed 進 repo，執行期不依賴任何 API。

## Users

`[推導]` 主要使用者是 25–60 歲的台灣散戶，在手機上、多半在做一個具體且時間敏感的財務決策時搜尋進來：

- 寬限期即將到期或正在比較兩家銀行方案的房貸族
- 還沒買房、想先知道自己預算上限的準首購族
- 50–64 歲即將請領勞保老年年金的勞工，以及替父母查資料的子女
- 45–60 歲、手上有一筆錢在排退休時程的人
- 在 0050／006208／0056／00878／00919 之間猶豫的定期定額投資人
- 每年 4–5 月報稅、股利要勾哪個選項的存股族

共同情境：手機、單次任務、沒有登入意願、對「試算工具」已有被內容農場消耗過的不信任感。

## Product Purpose

把七個台灣人每年真的會遇到、但現有免費工具只回一個數字的財務決策，做成可以親手操作的互動模型。成功的定義不是「算出答案」，而是使用者離開時獲得一個他自己動手做出來的直覺，並且知道這個答案的假設是什麼。

## Positioning

`[推導]` 台灣的理財試算市場已被大量薄型 SEO 站與導購站佔滿（figure.tw、twhousecalc、0857.tw、uptogo 等）。它們的共同結構性缺陷不是缺功能，而是：

1. 輸出單點數字，不輸出分布或情境比較
2. 重新試算即銷毀前一次結果，結構上無法比較
3. 不公開公式與法源
4. 商業模式（貸款媒合、開戶導流）與「誠實告訴你不可行」直接衝突
5. 官方工具（勞保局、健保署、財政部）多為不可連結、不可書籤、無 RWD 的 postback 頁面

本套件的立場：不代辦、不媒合、不收集資料、全部計算在瀏覽器內完成、公開每一條公式與法源、資料不足時明確拒答。**「誠實」在這個品類裡就是最強的差異化，而不是加分項。**

關於第 4 點需要講清楚，因為本站確實會掛分潤連結：衝突的不是「有分潤」，而是「分潤能改變結論」。所以分潤在程式碼層級被隔離——全站離站連結只有一個出口 `assets/js/core/partners.js`，設定只有一個檔 `assets/data/partners.json`，計算模組從頭到尾沒有 import 過它們，那個模組也沒有任何 API 能問到某筆連結值多少錢，因此排序邏輯不可能讀到分潤高低。保險類別整類排除（保險法第 167 條之 1 第 3 項，罰鍰 90–900 萬，已有實際裁罰案例）。每一筆連結必填 `expires`，過期自動退回官網並標示活動結束。只要頁面真的送出過追蹤連結就自動掛上揭露元件，完整清單常駐於 `disclosure.html`。

## Operating Context

- 使用者多半從搜尋引擎單點進入某一個工具，不是從首頁逛進來
- 主要裝置是手機直式；375px 寬是設計與驗收的第一現場，不是降級目標
- 一次性任務為主，但情境需要能被存下、被比較、被傳給伴侶或行員（URL 編碼 + localStorage）
- 報稅、勞保、貸款資料極度敏感，「資料不離開瀏覽器」是採用的前提

## Capabilities and Constraints

**技術約束（硬性）**

- 100% 靜態，GitHub Pages 部署，無伺服器、無資料庫、無 API key
- 執行期不得依賴任何需金鑰的服務
- 所有持久化只用 localStorage 與 URL query string
- 首屏即可用、可完全離線運作

**已驗證的資料可用性**

- `[已驗證]` `https://www.twse.com.tw/rwd/*` 回傳 `Access-Control-Allow-Origin: *`，可從瀏覽器直接呼叫；STOCK_DAY 一次只回一個月、且為**未還原權息**價格
- `[已驗證]` `openapi.twse.com.tw` 與 `tpex.org.tw/openapi` 無 CORS 標頭，不使用
- `[已驗證]` Yahoo Finance 端點無 CORS 標頭，不使用
- `[已驗證]` GSAP 3.15.0 全部外掛（含 SplitText、DrawSVG、MorphSVG、Inertia）皆已免費並可自 jsDelivr 取得

**法規與參數（會變動，因此一律外部化）**

所有稅率、級距、費率、成數上限、給付係數置於有版本標記的 JSON 常數檔，每一項附法源條號與公告日期，UI 顯示資料版本徽章並允許使用者逐項覆寫。寫死法規常數等同慢性錯誤。

**明確不做**

- 不做帳號、不做雲端同步、不做即時報價
- 不做加密貨幣（受眾與台灣散戶定位不相容）
- 不模擬已被撤回的修法（二代健保年度累計制已於 2025-11-06 由行政院指示暫緩，只實作現行單筆起扣制）

## Brand Commitments

- 名稱：看得見的錢（visible-money-tw）
- 語言：繁體中文（台灣用語），數字與程式碼保持原樣
- 發佈：GitHub Pages，`fengfeng1021` 帳號

## Evidence on Hand

- 2026-08-03 完成的市場調研報告（14 個代理人、299 次工具呼叫），含競品逐一查證與 CORS 實測矩陣，存於 `_slate.json`
- 台灣證交所 STOCK_DAY / TWT49U / etfDiv 端點可用，可離線建置還原權息序列
- **沒有**任何真實使用者訪談、沒有流量數據、沒有客戶或推薦文。任何 UI 上出現的使用者見證、下載數、評價都必須視為捏造，一律不得出現。

## Product Principles

1. **輸出分布與情境，不輸出單點數字。** 使用者要的是「我離翻轉點多遠」，不是「答案是 47.2」。
2. **資料不足就拒答。** 樣本不夠、法規未上路、模型不適用時，顯示「這個問題現在回答不了」並給出可回答的範圍，而不是畫一個假的分布。
3. **每個數字都能攤開看公式與法源。** 對抗內容農場黑箱最直接的做法。
4. **動畫必須是論證本身，不是裝飾。** 如果一個動效無法用一句話說明它讓使用者理解了什麼，就刪掉。
5. **不做單向恐嚇也不做單向鼓吹。** 呈現壞情境時必須同時給誠實的對照，結論交還使用者。
6. **手機優先是驗收現場。** 375px 上不成立的視覺，桌機再漂亮也是失敗。

## Accessibility & Inclusion

- 全站尊重 `prefers-reduced-motion`：所有動效降級為即時重繪，資訊完整不減
- 文字對比 WCAG AA（內文 4.5:1、大字 3:1）
- 所有互動可鍵盤操作；滑桿有等效的數字輸入
- 圖表結論同時以文字敘述呈現，不讓視覺是唯一的資訊通道
- 目標族群含 50–65 歲使用者與代查的子女，預設字級不得為了美觀而縮小
